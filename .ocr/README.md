# OCR API-mode pilot rules

Empty extra-rule list (`{"rules": []}`). OCR still applies its bundled system rules.

This workflow is advisory. Do not add `OpenCodeReview` to required checks
while Juror remains review gate 3.
